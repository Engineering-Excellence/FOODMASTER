DROP TABLE BALANCE CASCADE CONSTRAINTS;

CREATE TABLE BALANCE
(
    ACCOUNTID   VARCHAR2(50)        NOT NULL,
    ASSET       NUMBER              NOT NULL,
    ACCINCOME   NUMBER    DEFAULT 0 NOT NULL
        CONSTRAINT CK_BALANCE_ACCINCOME CHECK (ACCINCOME >= 0),
    ACCEXPENSE  NUMBER    DEFAULT 0 NOT NULL
        CONSTRAINT CK_BALANCE_ACCEXPENSE CHECK (ACCEXPENSE >= 0),
    LASTUPDATED TIMESTAMP DEFAULT SYSDATE
);


ALTER TABLE BALANCE
    NOPARALLEL;
ALTER TABLE BALANCE
    PARALLEL;

INSERT INTO BALANCE (ACCOUNTID, ASSETS)
VALUES ('account', 100000000);


INSERT INTO BALANCE
VALUES ('account', 1000000, SYSDATE);



COMMIT;


SELECT *
FROM BALANCE
ORDER BY LASTUPDATED DESC;


INSERT INTO BALANCE
VALUES ('AACsdasdsdasC', 2345, SYSDATE);


DESC BALANCE;