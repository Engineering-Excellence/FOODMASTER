		
DROP TABLE BALANCE CASCADE CONSTRAINTS;

CREATE TABLE BALANCE
(
    ACCOUNTID   VARCHAR2(50) NOT NULL,
    ASSET      NUMBER NOT NULL,
    INCOME   	NUMBER,
    EXPENSE		NUMBER,
    LASTUPDATED TIMESTAMP DEFAULT SYSDATE
);

INSERT INTO BALANCE
VALUES ('account', 1000000,10000, 1000, SYSDATE);

COMMIT;

        
SELECT *
FROM BALANCE
ORDER BY LASTUPDATED DESC;
        
        
INSERT INTO BALANCE VALUES('AACsdasdsdasC', 2345, SYSDATE);        


DESC BALANCE;